# Team Forge: 4-Way Autonomous Zero-Conflict Workflow Guide

This guide details the automated 4-way synchronization architecture powering **Team Forge (ForgeDiagnostics)** across **Android Studio**, **Antigravity IDE**, **Google AI Studio**, and **GitHub**.

---

## 🏗️ Architecture Overview

```
                  ┌────────────────────────┐
                  │    Google AI Studio    │
                  │  (Gemini API & Prompts)│
                  └───────────┬────────────┘
                              │ .env & GitHub Secrets
                              ▼
┌──────────────────┐     ┌───────────┐     ┌──────────────────┐
│  Android Studio  │◄───►│ Local Repo│◄───►│  Antigravity IDE │
│ (Build/Emulator) │     │ Workspace │     │ (AI Pair Dev)    │
└──────────────────┘     └─────┬─────┘     └──────────────────┘
                               │
                               │ 2-Way Autonomous Daemon (Zero-Conflict)
                               ▼
                  ┌────────────────────────┐
                  │    GitHub Repository   │
                  │(CI/CD, Releases, Cloud)│
                  └────────────────────────┘
```

---

## ⚡ Key Automation Features

1. **Real-Time File-Watcher & Debouncing (Local Changes)**:
   - File changes made in **Android Studio** or **Antigravity IDE** are detected within milliseconds.
   - Smart 3-second debouncing prevents fragmented commits during multi-file saves or builds.
   - Automatically commits with structured timestamped metadata and pushes to GitHub.

2. **Zero-Conflict Auto-Stash & Auto-Rebase (Incoming Remote Changes)**:
   - Polls GitHub every 10 seconds for commits generated by GitHub Actions (e.g. `Forge Autonomous Guardian`), PR merges, or web edits.
   - Uses `git pull --rebase --autostash` to merge changes without creating merge commits or clobbering in-progress local work.
   - Built-in **Auto-Healer** resolves concurrent race conditions and non-fast-forward collisions gracefully.

3. **Dual-Layer Git Hooks**:
   - `post-commit` hook triggers immediate background push whenever manual commits are made via Android Studio Git UI or VSCode.
   - `post-merge` hook logs synchronization state.

4. **Environment & Secrets Integration**:
   - `local.properties`: Auto-configures local Android SDK path (`C:\Users\<user>\AppData\Local\Android\Sdk`) and signing keys.
   - `.env`: Holds `GEMINI_API_KEY` for Android Studio `BuildConfig` and Antigravity tooling.
   - `npm run connect`: Automatically synchronizes `GEMINI_API_KEY` between local `.env` and GitHub Actions Secrets via `gh secret set`.

---

## 🛠️ CLI Commands & Utilities

| Command | Action |
| :--- | :--- |
| `npm run sync:watch` | Starts the live foreground sync watcher with real-time logs. |
| `npm run sync:silent` | Launches the background sync daemon silently on Windows (no console window). |
| `npm run sync` | Runs a single immediate 2-way sync cycle. |
| `npm run sync:status` | Displays git status and latest commit history. |
| `npm run connect` | Interactive 4-way setup & AI Studio key synchronizer. |

---

## 🤖 Autonomous Merge Conflict Bot Collective

Team Forge includes a two-tier automated GitHub bot collective designed to scan, detect, resolve, and merge Pull Requests encountering Git merge conflicts.

```
┌──────────────────────────────────────────────────────────┐
│              Conflict Finder Bot (Bot 1)                 │
│  - Scans open PRs on schedule / push / PR events          │
│  - Detects merge conflicts against target base branch   │
│  - Labels PR with `has-merge-conflict`                   │
│  - Comments diagnostic list of conflicting files         │
│  - Dispatches Bot 2 via workflow_dispatch                │
└────────────────────────────┬─────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────┐
│        Conflict Resolver & Auto-Merger Bot (Bot 2)       │
│  - Fetches target PR head and base branches              │
│  - Merges base branch into PR branch                     │
│  - Identifies files with conflict markers (`<<<<<<<`)    │
│  - Resolves conflicts using Gemini AI API / Smart Engine │
│  - Verifies zero conflict markers remain                 │
│  - Runs linting & test suite                             │
│  - Commits & pushes clean resolution to PR branch       │
│  - Automatically merges PR into base branch              │
└──────────────────────────────────────────────────────────┘
```

### 1. Conflict Finder Bot (Bot 1)
- **Workflow**: `.github/workflows/conflict_finder_bot.yml`
- **Script**: `scripts/conflict_finder.py`
- **Schedule**: Scans open PRs every 15 minutes or on PR open/synchronize events.
- **Actions**: Labels conflicting PRs, posts comments detailing affected files, and triggers Bot 2.

### 2. Conflict Resolver & Auto-Merger Bot (Bot 2)
- **Workflow**: `.github/workflows/conflict_resolver_bot.yml`
- **Script**: `scripts/conflict_resolver_brain.py`
- **Triggers**: `workflow_dispatch` or `repository_dispatch` with PR parameters (`pr_number`, `head_branch`, `base_branch`).
- **Actions**: Merges branches, resolves conflict markers with Gemini 2.5 Flash AI, validates syntax and zero conflict markers, commits, pushes, and auto-merges the PR.

---

## 📱 Opening in Android Studio
1. Launch Android Studio.
2. Select **Open** and choose:
   `c:\Users\michael\Desktop\final version\ForgeDiagnostics- repo final\ForgeDiagnostics-main`
3. Gradle will sync automatically using the configured `local.properties` and `.env`.
